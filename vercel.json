{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/property/(?P<id>[^/]+)",
      "dest": "/api/render?id=$id"
    },
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
